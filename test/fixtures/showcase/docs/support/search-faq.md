# Search FAQ

## /v1/search errors

If search requests fail or reindex jobs lag, support should verify the current index status and retry guidance.

## Reindex troubleshooting

Escalate when `/v1/search/reindex` returns errors or the index remains stale after a deploy.
