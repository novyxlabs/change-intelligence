# Billing Guide

## createCheckoutSession

Use `createCheckoutSession` to start a checkout flow for a pricing plan.

Current inputs:

- `planId`

This page explains the checkout flow, billing plan selection, and checkout URL handling.
